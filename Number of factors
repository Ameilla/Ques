#include<stdio.h>
main()
{
    int n,i,c=0;
    printf("Enter the number: ");
    scanf("%d",&n);
    printf("Factors= ");
    for(i=1;i<=n;i++)
    {
        if(n%i==0)
        {
            c++;
            printf("%d ",i);
        }
    }
    printf("\nNumber of factors= %d",c);
}
