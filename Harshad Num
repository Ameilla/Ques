#include<stdio.h>
main()
{
    int n;
    scanf("%d",&n);

    int num;
    num=n;
    int sum=0;

    while(num>0)
    {
        sum=sum+(num%10);
        num=num/10;

    }
    if(n%sum==0)
        printf("%d is Harshad number ",n);
    else
        printf("%d is not Harshad number",n);

}
