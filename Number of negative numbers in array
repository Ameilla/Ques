#include<stdio.h>
main()
{
    int n,i,a[10],neg=0;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=1;i<=n;i++)
    {
        if(a[i]<0)
        {
            neg++;
        }
    }
    printf("Number of negative numbers in Array element= %d",neg);
}
